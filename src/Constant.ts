export enum Task {
    UpgradeController ="UpgradeController",
    Pickup ="Pickup",
    Harvest ="Harvest",
    Transfer ="Transfer",
    Build = "Build",
    Repair = "Repair",
    Withdraw = "Withdraw",
    Idle = "Idle",
}

export enum Roler {
    Worker = "Worker",
}

export enum State {
    Unknown,
}
