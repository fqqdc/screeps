export enum Task {
    Harvest,
    UpgradeController,
    None,
    Transfer,
    Build,
    Pickup,
    Repair,
}

export enum State {
    Harvest,
    Transfer,
    Withdraw,
    UpgradeController,
    Idle,
    Build,
    Pickup,
    Repair,
}

export enum Roler {
    Worker = "worker",
}
