export enum Task {
    UpgradeController ="UpgradeController",
    Pickup ="Pickup",
    Harvest ="Harvest",
    Transfer ="Transfer",
    Build = "Build",
    Repair = "Repair",
    Withdraw = "Withdraw",
    Idle = "Idle",
}

export enum Automatic {
    None = "None",
    Harvest = "Harvest",
}

export enum Roler {
    Worker = "Worker",
}
